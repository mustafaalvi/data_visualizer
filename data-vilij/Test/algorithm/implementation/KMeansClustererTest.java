package algorithm.implementation;

import org.junit.Test;

import static org.junit.Assert.*;

public class KMeansClustererTest {


    @Test
    public void run() {
        /*
        KMeansClusterer cluster = new KMeansClusterer(dataSet, maxIt, updateInt, isContinous, numOfClusters);
        Thread algorithmnThread = new Thread(cluster);
        algorithmnThread.setDaemon(true);
        // chart.getData().clear();
        algorithmnThread.start();
        */
    }
}